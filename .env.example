# Copiar a .env.local (no subir a Git)
# URL de la API — debe terminar en /api

# Desarrollo local (HTTP)
VITE_API_BASE_URL=http://localhost:56398/api

# Desarrollo local (HTTPS)
# VITE_API_BASE_URL=https://localhost:56397/api

# Producción Railway (ejemplo — cambiar por tu dominio real)
# VITE_API_BASE_URL=https://backend-facturas-production.up.railway.app/api
